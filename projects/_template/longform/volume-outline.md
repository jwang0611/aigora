# Volume Outline — <题名> vol <M>: <卷名>

**Cap**: 5,000字（loading-manifest budget）
**Locked at**: V.OUTLINE checkpoint (see decisions.md). In-volume beat deviations allowed unless
they change the exit state (then EditorInChief sign-off + amendment note here).
**depends-on**: <≤3 digests this volume's drafting loads, e.g. vol-03, vol-05. Set by the
EditorInChief.>
**Monotonic quantities（单调量声明）**: <quantities that may only rise or only fall across this
volume, e.g. 水位只升、倒计时只降、日期只进. The seam audit and V.AUDIT check against these.>

## Volume Arc（卷弧）

- **Volume question（卷级问题）**: <opened in the first chapters>
- **Entry state**: <where/when/who-knows-what, from the previous volume's exit>
- **Mid-volume turn（卷中转折）**:
- **Volume climax → exit state**: <what is permanently changed; the book-level step this climax takes>
- **Volume-end hook（卷末钩）**:
- **Promises due this volume**: <registry IDs — pay or formally defer, never silently>

## Beats（逐 beat, ~one per chapter）

<For each beat: what changes and what it costs. `cast:` selects the entity files loaded when
drafting that chapter (≤5). `promises:` marks plants/escalations/payoffs by registry ID.
`entry:`/`exit:` are the physical-state contracts — MANDATORY when this volume is
parallel-drafted (`/LONGFORM.md`, Drafting Modes), recommended always: the seam audit and
V.AUDIT verify chapters against them, and the next beat's `entry:` must match this beat's
`exit:`.>

### ch NNN <题名>
- **Situation → change**:
- **Reader learns / feels**:
- **Ledger-critical facts**:
- **entry**: <时辰/倒计时 · 地点 · 在场者 · 关键物理量 · 关键道具持有>
- **exit**: <same fields at chapter close; next beat's entry must match>
- **cast**: <名, 名, ...>
- **promises**: <PROM-014 pay / PROM-021 plant / — >

### ch NNN+1 ...

## Key Moments（本卷必须存在的时刻）

<Locked by the EditorInChief. Drafting may move them, not cut them.>
