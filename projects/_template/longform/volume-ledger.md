# Volume Ledger — <题名> vol <M>

<!-- Cap: 10,000字 (worst case at volume end). Volume-scoped: this ledger covers ONLY this
     volume's chapters. Append-only during drafting; updated after EVERY chapter draft.
     KnowledgeAuditor audits chapter drafts against this file + entity files + open promises.

     AT V.COMPACT this file is archived: every line gets exactly one disposition (unpaid
     PROMISE → promise-registry; load-bearing fact → entity file or timeline; live KNOWS →
     entity file; rest stays), then the ARCHIVED header below is uncommented. -->

<!-- ARCHIVED at V.COMPACT <date> — NEVER load into drafting contexts. Facts from this volume
     now live in memory/digests/vol-M-digest.md, memory/entities/, memory/promise-registry.md,
     memory/timeline.md. Retro-edit amendments go in the Amendments section below. -->

Format, one line per fact:

`[ch NNN] <fact>` — established facts (dates, ages, injuries, objects, distances, rules invoked)
`[ch NNN] KNOWS: <who> knows <what> (learned how)` — knowledge states
`[ch NNN] PROMISE: <setup the reader will expect paid off>` — Chekhov items; ✓ when paid
`[ch NNN] BURN: <cumulative 字数> / milestones consumed: <M1 ✓, M2 …>` — one per chapter

---

## Facts

## Knowledge States

## Promises to the Reader

## Burn-Rate Lines

## Amendments（回溯修订注记 — only after archival, from retro-edits）

<!-- [retro <date>, commit <sha>, MF-ID] <what changed and why> -->
