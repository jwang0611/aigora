# Continuity Ledger — <Title>

Append-only. Updated after EVERY chapter draft. This file—not the context window—is the memory.
KnowledgeAuditor audits drafts against this ledger.

Format, one line per fact:

`[ch N] <fact>` — established facts (dates, ages, injuries, objects, distances, rules invoked)
`[ch N] KNOWS: <who> knows <what> (learned how)` — knowledge states
`[ch N] PROMISE: <setup the reader will expect paid off>` — Chekhov items; mark ✓ when paid off

---

## Facts

## Knowledge States

## Promises to the Reader
